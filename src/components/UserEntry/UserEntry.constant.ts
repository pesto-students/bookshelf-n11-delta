export const enum UserEntryState {
  Login = 1,
  SignUp,
}

export const MIN_PASSWORD_LENGTH = 6;
export const PASSWORD_MIN_LENGTH_MSG = `Password should be of minimum ${MIN_PASSWORD_LENGTH} characters length`;
